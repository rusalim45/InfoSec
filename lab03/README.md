Lab 3: Toy Shell
This lab creates a simple Python-based toy shell that lists files and folders older than a given date. Commands:

toy-shell> YYYY-MM-DD → list files/folders older than date
toy-shell> exit → quit shell
